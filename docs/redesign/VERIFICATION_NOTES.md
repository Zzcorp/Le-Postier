# Verification notes (auto-collected from implementation agents)


## foundation

**Files:** C:/Users/mathe/Documents/CODING/Empire/le-postier/static/css/tokens.css (new); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/css/base.css (rewritten); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/css/components.css (new); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/images/ui/postmark.svg (new); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/js/main.js (rewritten); C:/Users/mathe/Documents/CODING/Empire/le-postier/templates/base.html (restructured); C:/Users/mathe/Documents/CODING/Empire/le-postier/templates/partials/navbar.html (rebuilt); C:/Users/mathe/Documents/CODING/Empire/le-postier/templates/partials/postcard_frame.html (new); C:/Users/mathe/Documents/CODING/Empire/le-postier/templates/partials/pagination.html (new); C:/Users/mathe/Documents/CODING/Empire/le-postier/templates/partials/consent.html (new); C:/Users/mathe/Documents/CODING/Empire/le-postier/templates/partials/postcard_modal.html (rebuilt from 0-byte file); C:/Users/mathe/Documents/CODING/Empire/le-postier/templates/admin_sync_ovh.html (deleted)

- Page workstreams must NOT compensate for the fixed navbar: base.css sets body{padding-top:var(--nav-h)}. Full-bleed cabinet heroes (home) need margin-top:calc(-1*var(--nav-h)) in their page CSS if they want to sit under the bar.
- pagination.html uses the Django 5.1+ {% querystring %} tag — requires django.template.context_processors.request (already required by navbar's request.resolver_match). Verify the deploy workstream's rewritten settings keeps the request context processor.
- Vitrine contract: pages open it via window.Vitrine.open(id, orderedIdArray) by intercepting clicks on .pc-frame[data-id]; main.js deliberately adds NO default [data-id] click handler (per MIGRATION_PLAN §2). Verify browse/animated pages wire this.
- Like sync relies on the API shape {success, liked, likes_count} from POST /api/postcard/<id>/like/ and on the view returning 401/403 (not a 302 to HTML) for anonymous/forbidden — postJSON treats non-JSON as an error and shows a toast either way.
- Vitrine render expects the §3.3 detail shape incl. rarity slugs 'commune'/'rare'/'tres_rare' (badge CSS also covers model slugs common/very_rare used by the template partial). Confirm backend's rarity_label/rarity fields once views.py lands.
- postcard_frame.html hardcodes img width=600 height=400 with CSS aspect-ratio 3/2 + object-fit:contain — verify real vignette proportions look right; CLS is protected regardless.
- Desktop logout is a plain navbar link to {% url 'logout' %} (GET) matching the current view; if backend converts logout to POST-only, navbar needs a small form.
- Footer legal links Mentions légales/Confidentialité point to /contact/ with a title attribute as instructed — replace when real pages exist.
- Consent stores 'granted'/'denied' under localStorage key 'consent-ga'; GA G-YZX5PNSJZG is injected exclusively by main.js loadAnalytics(). Grep confirm no other gtag reference remains after page workstreams finish.
- Old orphan CSS (browse/contact/home/presentation/gallery.css) and browse.js/gallery.js are owned by page workstreams for deletion — intentionally untouched here.
- postmark.svg is consumed via CSS mask-image (stamp + footer watermark) so tinting works; the intro page (home-group) can inline it for the stroke-dashoffset draw animation.
- Verify visually: dentelure notches at footer top show the vélin through; mobile cabinet overlay focus trap and Esc; vitrine zoom pinch on a touch device; flip announcement 'Verso affiché' in a screen reader.

## deploy

**Files:** C:/Users/mathe/Documents/CODING/Empire/le-postier/le_postier/settings.py; C:/Users/mathe/Documents/CODING/Empire/le-postier/manage.py; C:/Users/mathe/Documents/CODING/Empire/le-postier/requirements.txt; C:/Users/mathe/Documents/CODING/Empire/le-postier/Dockerfile; C:/Users/mathe/Documents/CODING/Empire/le-postier/docker-compose.yml (new); C:/Users/mathe/Documents/CODING/Empire/le-postier/deploy/entrypoint.sh (new); C:/Users/mathe/Documents/CODING/Empire/le-postier/deploy/nginx.conf (new); C:/Users/mathe/Documents/CODING/Empire/le-postier/deploy/backup.sh (new); C:/Users/mathe/Documents/CODING/Empire/le-postier/.env.example (new); C:/Users/mathe/Documents/CODING/Empire/le-postier/.env (new, gitignored); C:/Users/mathe/Documents/CODING/Empire/le-postier/.gitignore (new); C:/Users/mathe/Documents/CODING/Empire/le-postier/.dockerignore (new); C:/Users/mathe/Documents/CODING/Empire/le-postier/DEPLOY_OVH.md (new); C:/Users/mathe/Documents/CODING/Empire/le-postier/README.md; C:/Users/mathe/Documents/CODING/Empire/le-postier/data/db_cp_16_02_2024.csv (moved from root); C:/Users/mathe/Documents/CODING/Empire/le-postier/data/export-251221_db.csv (moved from root); C:/Users/mathe/Documents/CODING/Empire/le-postier/runtime.txt (deleted); C:/Users/mathe/Documents/CODING/Empire/le-postier/start.sh (deleted); C:/Users/mathe/Documents/CODING/Empire/le-postier/build.sh (deleted); C:/Users/mathe/Documents/CODING/Empire/le-postier/fix_deployment.sh (deleted); C:/Users/mathe/Documents/CODING/Empire/le-postier/sync_images.sh (deleted); C:/Users/mathe/Documents/CODING/Empire/le-postier/simple_app.py (deleted); C:/Users/mathe/Documents/CODING/Empire/le-postier/le_postier/settings_production.py (deleted); C:/Users/mathe/Documents/CODING/Empire/le-postier/scripts/ (deleted, 5 files)

- settings.py imports cleanly with the committed local .env (verified: DEBUG=True, sqlite, whitenoise storage, MediaServeMiddleware absent) — but `manage.py check --deploy` must wait for the backend workstream to finish core/middleware.py (AnalyticsTrackingMiddleware is still referenced in MIDDLEWARE) and core/urls.py.
- SECRET_KEY has no default: any environment without .env (e.g. CI, a fresh clone) will raise decouple.UndefinedValueError by design — verification must run with a .env present.
- The compose web healthcheck GETs http://127.0.0.1:8000/robots.txt with X-Forwarded-Proto: https and needs 127.0.0.1 in ALLOWED_HOSTS (documented in .env.example and DEPLOY_OVH.md §5) AND the backend's robots.txt fix (template rename) — if backend ships a different robots URL, adjust the healthcheck path.
- Entrypoint runs `migrate --noinput` + `collectstatic --noinput` at container start; collectstatic writes to /app/staticfiles inside the container (not a volume) — regenerated on every start, intentional.
- gunicorn --timeout 60: verify no admin endpoint (export, media stats) legitimately exceeds 60s after the backend perf work; raise in deploy/entrypoint.sh if so.
- nginx.conf hardcodes collections.samathey.fr (server_name + cert paths) — expected per plan; TLS bootstrap in DEPLOY_OVH.md §6 uses `docker compose run --rm -p 80:80 --entrypoint certbot certbot certonly --standalone` BEFORE first `up -d`; certbot renew-loop then uses the webroot. Verify this ordering survives in end-to-end deploy testing.
- docker-compose.yml could not be YAML-parsed locally (no PyYAML in venv) — run `docker compose config` during Phase C.
- requirements.txt switched psycopg2-binary → psycopg[binary] (psycopg3); dj-database-url supports it (postgres:// URLs map to the django.db.backends.postgresql engine which uses psycopg3 automatically in Django 5.2) — confirm on the real Postgres connection.
- deploy/*.sh are LF and sh -n clean; Dockerfile additionally strips CR from entrypoint.sh and DEPLOY_OVH.md §5 strips both scripts after clone (repo lives on Windows).
- README quickstart references `manage.py import_csv` and `manage.py rebuild_media_index`, and DEPLOY_OVH.md references `rebuild_media_index --check` + `aggregate_analytics` — all owned by the backend workstream; verify the final command names/flags match.
- data/ still exists in the working tree with the two personal-data CSVs (needed for local import) but is gitignored — verify it never lands in git history; if the repo already tracked the CSVs at root, a `git rm --cached` of the old paths happens automatically since the files were moved, but history scrubbing (if required) is out of scope.
- Deleted le_postier/settings_production.py contained the old committed SECRET_KEY; the runbook mandates a freshly generated key in production — session/token invalidation on cutover is documented (DEPLOY_OVH.md §5 and §12).

## deploy-bis

**Files:** C:/Users/mathe/Documents/CODING/Empire/le-postier/le_postier/settings.py; C:/Users/mathe/Documents/CODING/Empire/le-postier/manage.py; C:/Users/mathe/Documents/CODING/Empire/le-postier/requirements.txt; C:/Users/mathe/Documents/CODING/Empire/le-postier/Dockerfile; C:/Users/mathe/Documents/CODING/Empire/le-postier/docker-compose.yml; C:/Users/mathe/Documents/CODING/Empire/le-postier/deploy/entrypoint.sh; C:/Users/mathe/Documents/CODING/Empire/le-postier/deploy/nginx.conf; C:/Users/mathe/Documents/CODING/Empire/le-postier/deploy/backup.sh; C:/Users/mathe/Documents/CODING/Empire/le-postier/.env.example; C:/Users/mathe/Documents/CODING/Empire/le-postier/.env; C:/Users/mathe/Documents/CODING/Empire/le-postier/.gitignore; C:/Users/mathe/Documents/CODING/Empire/le-postier/.dockerignore; C:/Users/mathe/Documents/CODING/Empire/le-postier/DEPLOY_OVH.md; C:/Users/mathe/Documents/CODING/Empire/le-postier/README.md; C:/Users/mathe/Documents/CODING/Empire/le-postier/data/db_cp_16_02_2024.csv (moved from root); C:/Users/mathe/Documents/CODING/Empire/le-postier/data/export-251221_db.csv (moved from root); DELETED: start.sh, build.sh, fix_deployment.sh, sync_images.sh, simple_app.py, runtime.txt, le_postier/settings_production.py, scripts/ (verified absent)

- Only settings.py was edited THIS session (MEDIA_ROOT empty-string fix); all other deliverables pre-existed from an earlier run of this workstream and were verified, not rewritten — diff review should focus on settings.py.
- compose web healthcheck GETs http://127.0.0.1:8000/robots.txt with X-Forwarded-Proto: https — it depends on the backend workstream's robot.txt.txt → robots.txt rename landing, and on prod ALLOWED_HOSTS including 127.0.0.1 (documented in .env.example and DEPLOY_OVH.md §5); if backend's rename slips, web will report unhealthy.
- DEPLOY_OVH.md and README.md reference backend-owned management commands by name: import_csv, rebuild_media_index (incl. --check), aggregate_analytics — verify those exact names ship in Phase A backend output.
- manage.py check --deploy in Phase C requires SECRET_KEY (env-mandatory, no default): run it from the project root so decouple finds .env; the committed local .env provides DEBUG=True + a fresh key + empty DATABASE_URL, so fresh-sqlite migrate/runserver should work immediately.
- Whitenoise CompressedManifestStaticFilesStorage: hashed URLs are bypassed when DEBUG=True (HashedFilesMixin._url), so local dev needs no collectstatic; in production the entrypoint runs collectstatic before gunicorn — verify entrypoint output in Phase C container smoke if one is run.
- Shell scripts (deploy/*.sh) are authored on Windows; CRLF is neutralized twice (Dockerfile sed on entrypoint.sh at build, DEPLOY_OVH.md §5 sed on deploy/*.sh after clone) — backup.sh runs on the HOST via cron, so the §5 sed step is load-bearing; worth confirming the runbook step wasn't skipped in any future doc edits.
- certbot renewal is a compose renew-loop container (per MIGRATION_PLAN §4) rather than a cron certbot line; the §9 cron reloads nginx weekly to pick up rotated certs — verify this satisfies the reviewer's reading of 'certbot renew' in the cron contract.
- SECURE_PROXY_SSL_HEADER is set unconditionally (harmless under runserver since SECURE_SSL_REDIRECT=False when DEBUG); HSTS starts at 3600 via env SECURE_HSTS_SECONDS with the raise-to-31536000 procedure in DEPLOY_OVH.md §12.
- DATA_UPLOAD_MAX_MEMORY_SIZE is also 10MB alongside FILE_UPLOAD_MAX_MEMORY_SIZE — multipart file payloads do not count toward DATA_UPLOAD_MAX_MEMORY_SIZE in Django, so 200MB admin video uploads still pass; confirm no admin form posts >10MB of non-file field data.
- Grep for RENDER/onrender//var/data/settings_production across the repo (excluding docs/ and .venv/) returns only the intentional <EXTERNAL_DATABASE_URL_RENDER> placeholder in DEPLOY_OVH.md §8.1 — core/ hits listed in the audit are the backend workstream's to remove, not mine.

## backend

**Files:** C:/Users/mathe/Documents/CODING/Empire/le-postier/core/views.py; C:/Users/mathe/Documents/CODING/Empire/le-postier/core/models.py; C:/Users/mathe/Documents/CODING/Empire/le-postier/core/urls.py; C:/Users/mathe/Documents/CODING/Empire/le-postier/le_postier/urls.py; C:/Users/mathe/Documents/CODING/Empire/le-postier/core/middleware.py; C:/Users/mathe/Documents/CODING/Empire/le-postier/core/utils.py; C:/Users/mathe/Documents/CODING/Empire/le-postier/core/admin.py; C:/Users/mathe/Documents/CODING/Empire/le-postier/core/migrations/0008_media_cache.py; C:/Users/mathe/Documents/CODING/Empire/le-postier/core/management/commands/rebuild_media_index.py; C:/Users/mathe/Documents/CODING/Empire/le-postier/core/management/commands/aggregate_analytics.py; C:/Users/mathe/Documents/CODING/Empire/le-postier/core/management/commands/import_csv.py; C:/Users/mathe/Documents/CODING/Empire/le-postier/core/management/commands/check_media.py; C:/Users/mathe/Documents/CODING/Empire/le-postier/templates/robots.txt; C:/Users/mathe/Documents/CODING/Empire/le-postier/core/management/commands/ (deleted ~30 Render/FTP-era, duplicate importer, and junk commands; kept __init__.py, import_csv.py, export_to_csv.py, check_media.py, rebuild_media_index.py, aggregate_analytics.py); C:/Users/mathe/Documents/CODING/Empire/le-postier/core/import_csv_update.py (deleted); C:/Users/mathe/Documents/CODING/Empire/le-postier/core/templatestags/ (deleted)

- Run migration 0008 on a fresh sqlite DB and confirm it matches models exactly: `manage.py makemigrations --check --dry-run` must report no missing migrations (could not be run this phase per instructions).
- GET /robots.txt must return 200 text/plain (compose healthcheck depends on it) — template is templates/robots.txt, view passes site_url from settings.SITE_URL (default https://collections.samathey.fr).
- Smoke-test /parcourir/ pagination edge cases: ?q with accents, ?rarete=tres_rare, ?tri=populaires|recentes, legacy ?search=/keywords_input= params, out-of-range ?page.
- Verify /api/postcard/<id>/ locked behavior for anonymous users on rare and very_rare cards (locked:true, vignette only, empty grande/dos/zoom/animations) and that views_count increments via F().
- upload_cover URL branch changed this session: it now only accepts same-host /media/... URLs and copies from disk (SSRF closed). Confirm the profile cover 'choose a postcard' flow passes the picker's vignette_url and still works; file-upload branch unchanged.
- search_users now returns an extra category_label field alongside username/category — page workstreams should use it instead of the slug.
- After placing placeholder media, run `manage.py rebuild_media_index --check` then without --check; confirm has_images/has_animation counts and that browse/animated pages render with zero disk I/O.
- aggregate_analytics: verify DailyAnalytics/HourlyAnalytics fill for past days only and that raw rows older than 90 days are pruned while IPLocation rows survive.
- Analytics middleware geo lookups are cache-only with a daemon-thread refresh (utils.fetch_and_cache_location); under runserver, first request from a new IP records empty location and a later request gets the cached one — expected behavior, not a bug.
- admin_detailed_stats_api no longer computes hourly/weekly pattern or postcards_most_viewed (they were never in the JSON payload; the weekly query used Postgres-only EXTRACT(DOW) and would have crashed SQLite). Confirm the admin dashboard template/JS never referenced those keys.
- manage.py check passes with 0 issues; py_compile passes on every owned .py. Do not trust `manage.py check --deploy` yet — that is Phase C.

## page:browse

**Files:** C:/Users/mathe/Documents/CODING/Empire/le-postier/templates/browse.html (rebuilt — prior session of this workstream, verified now); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/css/pages/browse.css (new — prior session of this workstream, verified now); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/js/browse.js (rewritten); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/css/browse.css (deleted); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/css/gallery.css (deleted); C:/Users/mathe/Documents/CODING/Empire/le-postier/templates/gallery.html (deleted)

- Deep link: /parcourir/?carte=<id> must open the vitrine on load; the legacy ?postcard=<id> (used by partials/postcard_frame.html no-JS href) must do the same and be normalised to ?carte in the address bar via replaceState; closing the vitrine must remove the param without reloading.
- Vitrine traversal on browse uses only the current page's ids from the inline #browse-page-ids JSON script — confirm no full-catalogue data appears in the rendered HTML (grep the response for animation/zoom/grande URLs: only vignette URLs should be present in the grid).
- Filter/sort drawer submits as GET with names q/rarete/tri and no page input, so applying filters resets to page 1; server (core/views.py browse) validates rarete in {commune,rare,tres_rare} and tri in {numero,populaires,recentes}.
- browse.html uses {% querystring %} (Django 5.1+) in chip-dismiss links and the drawer's Réinitialiser link — requires django.template.context_processors.request; verify it renders under the final settings.
- Skeleton wall: submitting the search form or clicking a pagination link/filter chip hides #browse-grid and reveals #browse-skeleton; verify the real grid is restored when navigating back via the back/forward cache (pageshow persisted).
- Active-filter dot on the drawer toggle only shows when rarete or tri != numero (the old always-on green-dot bug must be gone).
- The 'Récentes' sort orders by -created_at server-side; confirm with fixture data once CSV import runs in Phase C.
- Empty state renders when a search yields no results ('Désolé, aucune carte postale trouvée' with a Réinitialiser la recherche secondary button) — check with a nonsense query.
- Like buttons inside the grid are handled by main.js's delegated .pc-like handler, not browse.js — verify a like on a grid tile stamps the postmark and stays in sync with the vitrine's like button.
- Deleted files (static/css/browse.css, static/css/gallery.css, templates/gallery.html) have no remaining references anywhere; the gallery view/route were removed by the backend workstream.

## page:admin

**Files:** C:/Users/mathe/Documents/CODING/Empire/le-postier/templates/admin_dashboard.html (edited: {% now %} fix; rest was rebuilt in prior session of this workstream); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/css/admin_dashboard.css (full rewrite); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/js/admin_dashboard.js (new)

- static/js/admin_dashboard.js is a NEW file not listed in MIGRATION_PLAN §1 ownership — the template's extra_js references it (created here because the per-page-JS rule requires inline scripts to move out); confirm no other workstream creates the same path.
- Dashboard requires staff login: smoke /tableau-de-bord/ as admin; verify all four charts render from the #dashboard-data JSON script (daily_stats/hourly_traffic are json.dumps strings injected with |safe — confirm non-empty valid JSON on a fresh DB where they will be lists of zeros).
- Exercise the mutating flows against a dev DB: add postcard (French error surfacing on duplicate number), user category PUT (select reverts on failure), user DELETE (row removal), suggestion status PUT (tag label + class update).
- Modal data flows: click a visitor / IP icon (IP lookup does a blocking geo fetch on cache miss — first click may be slow), a country row, a user analytics eye, and a mini postcard card; all content is DOM-built — check layout of .dlg-* classes at <768px.
- The .admin block uses margin-top: calc(-1*var(--nav-h)) for the full-bleed cabinet ground: verify the vélin glass navbar over dark content is acceptable, and that the vélin gap between the admin ground and the footer (footer has margin-top: var(--sp-9) in components.css, foundation-owned) reads intentionally.
- Realtime polling: 30s interval preserved, skipped while document.hidden — verify visitor cards rebuilt via JS match the server-rendered markup (visitor-geo/place/page/tech/user classes).
- Reduced motion: Chart.js animations disabled via matchMedia; base.css global kill switch covers CSS transitions.
- Charts legend counts in the template are server-rendered ints (user_categories/device_breakdown); doughnut slice order matches legend dot order (slots 1..4).

## page:social

**Files:** C:/Users/mathe/Documents/CODING/Empire/le-postier/static/js/la-poste.js (new); C:/Users/mathe/Documents/CODING/Empire/le-postier/templates/profile.html (rewritten); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/css/pages/profile.css (new); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/js/profile.js (new); C:/Users/mathe/Documents/CODING/Empire/le-postier/templates/la_poste.html (verified — produced by this workstream's prior session); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/css/pages/la-poste.css (verified — produced by this workstream's prior session)

- Runtime smoke of /la-poste/: compose end-to-end (pick card → message → stamp switch 10c→5c with >44 chars must warn, not truncate → send private and public) and confirm the postmark strike plays then the Envoyées grid / Mur update without reload.
- Verify unread flow: reading an unread card removes the card badge and decrements BOTH the page pill (#lp-unread-pill) and the navbar/menu-cabinet La Poste pill-counts (selector '.navbar-link .pill-count, .menu-cabinet-link .pill-count' — if navbar markup changes, this selector must follow).
- Deep links: /la-poste/?to=<username> opens compose with recipient prefilled; /la-poste/?postcard=<id> opens compose at step 2 with the card preselected (relies on data-preselect-* attrs the view fills). Both are skipped when the user has no signature (button disabled).
- Profile routes: /profil/, /profil/activite/, /profil/connexions/, /profil/favoris/, /profil/parametres/ all render profile.html with the right tab active; /utilisateur/<u>/ redirects to /la-poste/?to=.
- Cover 'choisir une carte' path: profile.js POSTs {cover_url: vignette_url} as JSON to /api/profile/cover/ — depends on upload_cover's JSON-body branch and same-host /media/ URL validation (views.py:984-1027). In DEBUG the vignette URLs must be /media/-prefixed for the check to pass.
- Drawn signature now exports a transparent PNG (no background fill) — visually confirm it renders correctly on the compose verso and as the avatar on the vélin ground.
- toggle_connection_favorite / update_connection_notes (/api/connection/<id>/...) remain unwired: the consolidated profile context exposes annotated users, not UserConnection rows, so there is no connection_id to call them with — same dead-surface status as before the redesign; flagging for a backend decision (expose ids or drop the endpoints).
- search_users returns category_label (verified in views.py:1944-1953) — the autocomplete renders it; if that field is ever dropped the suggestion label degrades to empty, not to a raw slug.
- la-poste.js uses one optional-chaining expression and <template>/dialog APIs — matches the browser baseline already set by main.js (native <dialog>, :has() in CSS).
- Esc inside compose closes the recipient-suggestion list first (keydown preventDefault before the dialog cancel) — worth a manual keyboard pass.

## page:media

**Files:** C:/Users/mathe/Documents/CODING/Empire/le-postier/templates/animated_gallery.html (rewritten); C:/Users/mathe/Documents/CODING/Empire/le-postier/templates/decouvrir.html (rewritten); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/css/pages/animated.css (new); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/css/pages/decouvrir.css (new); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/js/animated.js (new); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/js/decouvrir.js (new); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/js/gallery.js (deleted)

- Like semantics mismatch (backend-owned): animated_gallery view builds user_likes with is_animated_like=True, but the shared main.js .pc-like handler posts JSON to /api/postcard/<id>/like/ and core/views.py like_postcard reads request.POST (form-encoded) so is_animated always defaults to false — new likes are recorded as non-animated while initial tile state on /cp-animes/ reads the animated set. Decide in Phase C whether like_postcard should parse the JSON body or the view should pass animated=False.
- Rickroll retirement is template-side only: core/views.py decouvrir still ships video_id 'dQw4w9WgXcQ' for « Accident de l'archevêché » (context contract 'unchanged'); decouvrir.html special-cases that exact id and renders a static « Animation en préparation » figure. Replacing the id in views.py with the real YouTube id will automatically re-enable the button/video flow.
- The animation-suggestion flow (« Suggérer une animation ») died with the old duplicate modal per the per-page brief; /api/postcard/<id>/suggest/ remains routed but is no longer reachable from any page — confirm intended or assign it a home.
- The shared Vitrine (main.js) plays only animation_urls[0]; cards with video_count > 1 have no video selector any more and the old multi-video badge was dropped from tiles — foundation-level behavior, verify acceptable.
- decouvrir now makes exactly two third-party references, both only after an explicit click on a painting: the injected youtube-nocookie.com/embed iframe and the youtube.com/watch fallback link. If a CSP is added at deploy, it needs frame-src https://www.youtube-nocookie.com.
- Dark-page rendering smoke: /cp-animes/ sets body background to --cab-bg from pages/animated.css (base.css order dependency) — check the strip under the fixed light navbar, the gap above the footer (intentionally dark, dentelure punches show --cab-bg), and that light toasts/Django messages remain legible on the dark ground.
- ?highlight=<id> on /cp-animes/ opens the Vitrine even when the card sits on another pagination page (single-card mode, arrows hidden) and the param is stripped from the URL and from pagination hrefs after opening — verify with a card from page 2+.
- No-JS fallbacks: animated tiles link to /parcourir/?postcard=<id> (frame partial contract) and decouvrir paintings are <button>s that do nothing without JS (title/cartel still fully visible); confirm acceptable in Phase C runserver smoke.

## page:home

**Files:** C:/Users/mathe/Documents/CODING/Empire/le-postier/templates/home.html (rewritten); C:/Users/mathe/Documents/CODING/Empire/le-postier/templates/intro.html (rewritten, standalone); C:/Users/mathe/Documents/CODING/Empire/le-postier/templates/presentation.html (rewritten); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/css/pages/home.css (new); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/css/pages/intro.css (new); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/css/pages/presentation.css (new); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/js/home.js (new); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/images/presentation_samathey.jpg (new — URL-safe copy of cp_bg_présentation.jpg); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/css/home.css (deleted); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/css/presentation.css (deleted)

- static/js/home.js is a NEW file not explicitly listed in MIGRATION_PLAN §1 for home-group, but required by the §2 contract (« Page JS opens the vitrine dialog by intercepting click on [data-id] ») — no other workstream owns it; confirm no collision.
- Home tiles render WITHOUT like buttons because the home view context has no user_likes annotation — the like lives in the vitrine (API returns liked state). Confirm this reading of DESIGN_SPEC §7 Accueil is acceptable.
- No-JS fallback for home frames is /browse/?postcard=<id> (href from partials/postcard_frame.html) — verify the browse workstream honors the ?postcard= deep link.
- Intro timing: motion path navigates at ~1.62s (leave at 1220ms + 400ms lift), reduced motion at ~0.9s after a 600ms static fade; skip = « Passer » anchor (works with JS disabled) + any keydown/pointerdown; noscript meta refresh at 1s. redirect_url is server-validated (safe_next_url) and escaped with |escapejs.
- Intro links ONLY tokens.css + pages/intro.css and carries its own reset/focus/reduced-motion; verify visually that the postmark stroke-draw and cabinet grain render (paper-grain path ../../images/ui/paper-grain.png from css/pages/).
- presentation_samathey.jpg is a copy — the accented original static/images/cp_bg_présentation.jpg still exists (referenced nowhere); a later cleanup phase may delete it.
- Stats numbers print without French thousand separators (humanize is not in INSTALLED_APPS; USE_THOUSAND_SEPARATOR unset) — '1862' not '1 862'. Add humanize in a later phase if the grouped form is wanted.
- Intentionally dropped copy per spec: home VideoCarousel + scroll cue « Découvrir », the welcome section's 3 route buttons (nav/footer cover those routes), and the intro fake-loading strings (fake loader abolished).
- Hero uses margin-top:calc(-1*var(--nav-h)) per foundation note (body already pads for the fixed navbar) — check no double compensation at 1024px breakpoint where --nav-h changes to 56px.
- manage.py check passed (0 issues); migrations were NOT run per instructions.

## page:auth

**Files:** C:/Users/mathe/Documents/CODING/Empire/le-postier/templates/login.html (rebuilt); C:/Users/mathe/Documents/CODING/Empire/le-postier/templates/register.html (rebuilt); C:/Users/mathe/Documents/CODING/Empire/le-postier/templates/verify_email.html (rebuilt); C:/Users/mathe/Documents/CODING/Empire/le-postier/templates/set_password.html (rebuilt); C:/Users/mathe/Documents/CODING/Empire/le-postier/templates/registration_complete.html (rebuilt); C:/Users/mathe/Documents/CODING/Empire/le-postier/templates/contact.html (rebuilt); C:/Users/mathe/Documents/CODING/Empire/le-postier/templates/emails/verification_code.html (restyled); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/css/pages/auth.css (extended — created in this workstream's earlier partial run); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/css/pages/contact.css (new); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/js/auth.js (new); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/js/contact.js (new); C:/Users/mathe/Documents/CODING/Empire/le-postier/static/css/contact.css (deleted — dead legacy file)

- Two NEW JS files (static/js/auth.js, static/js/contact.js) are not in MIGRATION_PLAN §1's ownership table for this workstream — created because the orchestrator's rule 'page JS via {% block extra_js %} with defer, no inline <script>' requires external files; no other workstream claims these paths, but confirm no collision in the merge review.
- verify_email flow depends on listener ordering: main.js (document-level code-cell wiring) + auth.js (form-level, deferred one tick via setTimeout(0)) must both load — manually test type/backspace/paste/submit on /verification/ and confirm the hidden #code field posts 6 digits; also test the <noscript> fallback (JS disabled → cells hidden, plain input posts; two inputs share name='code' and Django takes the last).
- Resend endpoint contract: auth.js postJSON to /verification/renvoyer/ expects JSON {success, message} and throws on 4xx/5xx — verify Toast messages appear for both success and session-expired (400) cases.
- Login form intentionally has NO action attribute so a POST to /connexion/?next=… preserves ?next (the view reads request.GET.get('next') and validates via safe_next_url) — confirm redirect-after-login works and no other code assumed the old action.
- Contact POST contract unchanged: only 'message' is consumed; the extra 'affranchissement' radio value rides along in POST and is ignored by ContactForm — confirm this is acceptable; the stamp only changes the displayed allowance (5c=300 soft cap, warning text in --error, never blocks or truncates; textarea hard maxlength stays 500).
- Rationing-law tension on contact success render: the postmarked « Bien reçue » card AND the verso-ruled textarea are both visible in the same view — both are explicitly required by the workstream brief; flag if the adversarial review wants the success card to suppress the form.
- set_password checklist is advisory only (submit never gated client-side); the 'trop courant' item uses a ~20-entry client subset of Django's 20k common-password list, so a password can pass the live check and still fail server-side — the server error list then renders in the role=alert box; confirm this UX is acceptable.
- CSS :has() is used for the stamp-option checked/focus states (with a JS .is-selected duplicate as fallback) — verify in the oldest supported browser matrix.
- Email template: verify rendering in Outlook (bgcolor fallbacks used everywhere, border-radius will be dropped harmlessly); plain-text alternative in views.py already says 'Collection Samathey' so branding now matches.
- Auth pages funnel steps (verify/set_password/registration_complete) emit <meta name='robots' content='noindex'> inside their {% block meta %} override — confirm SEO intent; login/register/contact stay indexable with full OG tags.
- Not testable without DB/media in this phase: actual funnel navigation (session-gated verify/set_password redirects), resend cooldown against the live endpoint, and Cpa look of Timbre_5c/10c PNGs in the stamp slot (aspect-ratio 5/6 slot assumes roughly portrait stamps — eyeball at runtime).